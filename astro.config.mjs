import { defineConfig } from "astro/config";

export default defineConfig({
  // Static output by default — no SSR needed.
  // Change the site URL to the production domain before deploying.
  site: "https://example.com",
});
