// ============================================================
// Site Configuration
// ============================================================
// Fill in the values below with the credentials you received.
// This is the ONLY file you need to edit to connect the
// contact form to the backend.
// ============================================================

/** The backend API endpoint URL. */
export const API_ENDPOINT = "https://gettrazo.app/api/v1/submissions";

/** The API key for this site (starts with sk_live_). */
export const API_KEY = "sk_live_REPLACE_ME";

/** The Cap site key (public). The widget endpoint is built from it. */
export const CAP_SITE_KEY = "REPLACE_ME";

/** The Cap server base URL. You should not need to change this. */
export const CAP_BASE_URL = "https://cap.gettrazo.app";

// ============================================================
// Site Metadata (used in the base layout <head>)
// ============================================================

export const SITE_NAME = "Client Site";
export const SITE_DESCRIPTION = "A website for our client.";
